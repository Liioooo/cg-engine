#ifndef TRANSFORMS_OFFSET_PC_GLSL
#define TRANSFORMS_OFFSET_PC_GLSL

#include "Macros.glsl"

PUSH_CONSTANT(TransformsOffset, 10) {
    int transformsOffset;
} pc_transformsOffset;

#endif // TRANSFORMS_OFFSET_PC_GLSL
